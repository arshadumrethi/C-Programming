#include <stdio.h>

/* This is a comment. */

int main(int argc, char *argv[])
{
    int distance = 100 * 100;
    int more = distance + 111;
    // this is also a comment

printf("You are %d miles away.\n", more);

    return 0;
}